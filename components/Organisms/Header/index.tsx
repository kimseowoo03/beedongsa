"use client";
import styled from "@emotion/styled";

const HeaderWrap = styled.div`
  height: 60px;
  background-color: black;
`;

const Header = () => {
  return <HeaderWrap>Header입니다.</HeaderWrap>;
};

export default Header;
